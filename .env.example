# Backend API base URL — must NOT end with a slash
NEXT_PUBLIC_API_URL="http://localhost:3001"

# JWT verification secret — MUST match the backend's JWT_SECRET
# (generate with: openssl rand -base64 32, and share the same value in both repos)
JWT_SECRET="replace-with-a-random-32-byte-secret"
